\# MIL 模型 Grad-CAM 可解释性分析代码说明文档



\## 1. 核心设计理念：内存高效（Memory-Efficient）

在传统的多实例学习（MIL）中，一个病人（Bag）可能包含数十甚至上百张病理/医学图像（Instances）。如果直接对整个 Bag 进行反向传播来计算 Grad-CAM，显存往往会直接溢出（OOM）。

这段代码的破局点在于\*\*“移花接木”\*\*：

\* 先在\*\*无梯度（`torch.no\_grad`）\*\*环境下计算所有图片的特征。

\* 筛选出注意力分数（Attention Score）最高的几张图（Top-K）。

\* 针对这几张目标图，\*\*仅对单张图片重新开启梯度计算\*\*，其余图片直接复用之前缓存的无梯度特征进行拼接，从而完美完成反向传播并提取 CAM。



\---



\## 2. 核心处理流程



代码的整体执行流程可以分为四个主要阶段：



\### 阶段一：数据加载与模型初始化

1\. \*\*参数解析与数据集构建\*\*：读取 CSV 标签和图像路径，使用 `PatientBagDataset` 构建数据集。

2\. \*\*批处理（Collate Function）\*\*：`collate\_patient` 函数负责将不同大小的 Bag 填充（Pad）到当前批次的最大长度，确保形状对齐（`B, N, C, H, W`）。

3\. \*\*模型挂载钩子（Hooks）\*\*：实例化 `MILGradCAM\_MemoryEfficient`。它会自动判断特征提取器是 ViT/DINOv2 还是普通 CNN，并在最后一层（如 ViT 的 `norm1` 或 CNN 的 `conv\_head`）注册前向（Forward）和反向（Backward）钩子，用于捕获激活值（Activations）和梯度（Gradients）。



\### 阶段二：全局无梯度快速摸底（Phase 1）

对于每个病人的数据（Bag）：

1\. 模型在 `torch.no\_grad()` 上下文中对所有图片进行前向传播，提取所有图片的特征 `feats\_all\_no\_grad`。

2\. 特征通过池化层（Pooling Layer）计算出注意力分数 `attn\_val`，并通过分类头得出最终的预测概率 `prob`。

3\. 如果设置了 `--only\_positive` 且预测概率小于 0.5，则直接跳过该病人，节省计算时间。

4\. 根据注意力分数，提取对预测贡献最大的前 K 张图片（Top-K）的索引。



\### 阶段三：单实例精准反向传播（Phase 2）

对筛选出的 Top-K 图片逐一进行处理：

1\. \*\*开启钩子抓取\*\*：将 `cam\_extractor.hook\_enabled` 设为 `True`。

2\. \*\*单图梯度计算\*\*：仅将当前的“目标图（Target Image）”输入 Encoder 并保留计算图获取特征 `f\_target`。

3\. \*\*特征拼接\*\*：将带有梯度的 `f\_target` 与阶段二中缓存的无梯度特征 `feats\_all\_no\_grad` 按原顺序拼接在一起。

4\. \*\*反向传播\*\*：将拼接后的特征送入 Attention Pooling 和 分类头，得到 `score`，并执行 `score.backward()`。此时，梯度只会流向目标图，钩子精准捕获该图的激活值和梯度。

5\. \*\*关闭钩子抓取\*\*：重置状态，防止干扰下一张图的计算。



\### 阶段四：CAM 计算与可视化

1\. `compute\_cam` 函数接收激活值和梯度。

&#x20;   \* \*\*对于 CNN\*\*：对梯度进行全局平均池化得到权重，将其与激活值加权求和。

&#x20;   \* \*\*对于 ViT\*\*：剔除 Class Token，提取空间 Token，重塑为 2D 矩阵（例如 14x14 或 16x16），并进行类似的加权求和。

2\. 使用 ReLU 激活函数保留正向影响，并进行归一化处理。

3\. 将生成的 Heatmap（热力图）放大至原图尺寸，与原图进行叠加融合（Overlay）。

4\. 使用 Matplotlib 绘制对比图，并保存到指定输出目录，文件名中包含 PID、预测得分、排名和注意力分数。



\---



\## 3. 关键函数/类详解



\* \*\*`collate\_patient`\*\*: 解决不同病人的图片数量不一致的问题，使用零填充（Zero-padding）对齐维度。

\* \*\*`MILGradCAM\_MemoryEfficient`\*\*: 整个架构的核心。包含 `hook\_enabled` 开关机制。在不该抓取的时候保持静默，在专门针对某一张图算 CAM 时才记录数据，避免了特征混淆。

\* \*\*`compute\_cam`\*\*: 兼容了 ViT 和 CNN 不同的特征维度。特别是 ViT 部分，包含了一段自动推断特征图空间分辨率（`H\_feat`）的逻辑。

